"""
Find the best available meeting room for each request.

Given rooms with capacities, existing bookings, and meeting requests,
assign each request the smallest-capacity room that fits and is not
booked during the requested time window, or None if unavailable.
"""


def find_rooms(rooms: list[dict], bookings: list[dict], requests: list[dict]) -> list:
    pass
